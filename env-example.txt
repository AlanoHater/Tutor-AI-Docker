@"
# Copia este archivo como '.env' y completa los valores

# Puerto del servidor backend
PORT=5000

# Entorno de ejecución (development/production)
NODE_ENV=production

# Ruta de la base de datos SQLite
DB_PATH=/app/database/tutor.db

# API Key de Google Gemini para generación de preguntas con IA
# IMPORTANTE: Esta es la clave necesaria para que funcione la IA
# OBTÉN TU PROPIA API KEY desde: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=tu_clave_api_de_gemini_aqui
"@ | Out-File -FilePath .env.example -Encoding UTF8
